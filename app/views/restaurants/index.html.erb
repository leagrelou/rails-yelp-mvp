<h1>List of restaurants</h1>

<p><%= link_to "New Restaurant", new_restaurant_path %></p>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>

<!-- (restaurant) is it a number? -->
